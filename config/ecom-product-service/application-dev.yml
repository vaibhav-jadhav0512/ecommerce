server:
  port: 9000
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-25813495.okta.com/oauth2/default
      client:
        registration:
          internal-client:
            provider: okta
            authorization-grant-type: client_credentials
            scope: internal
            client-id: 0oaao9o5zyIWHzLKy5d7
            client-secret: RYdeUmHnTeHROrzOuUMPYSx-QDHLoR3vAqdADj3qvYi-GoI-n_ZHO9rCSqrnGXXe
        provider:
          okta:
            issuer-uri: https://dev-25813495.okta.com/oauth2/default
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/product
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ECOM-PRODUCT-SERVICE
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
management:
  tracing:
    sampling:
      probability: '1.0'      
logging:
  level:
    org:
      springframework:
        web: DEBUG
okta:
  oauth2:
    issuer: https://dev-25813495.okta.com/oauth2/default
    audience: api://default
